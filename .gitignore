# Private project/collaboration config — never publish
CLAUDE.md

node_modules/
data/raw/*
# …but keep the hand-built curation inputs (metadata only — IDs/titles/tiers, no
# story text): the verified WB-owned set, the expansion list, the tiered manifest.
!data/raw/wb-owned.txt
!data/raw/gdocs-expansion.json
!data/raw/gdocs-manifest.json
data/corpus/
data/wog-batches/
data/wog-scores-parts/
data/attr-batches/
data/wog-serial-tags-parts/
# wog-scores.json (the Haiku classifications) IS committed: it's id -> scores with
# no story text, and re-running the ~$3 / ~50-min classification would be wasteful.
# Sonnet thread-attribution pass: keep the verified patch (wog-thread-fixes.json),
# ignore the regenerable intermediates.
data/wog-thread-batches/
data/wog-thread-parts/
data/wog-thread-context.json
Spacebattles WoG Repository/
build/
site/pagefind/
site/meta.json
site/listings/
.wrangler/
*.log
.DS_Store

data/wog-served-untagged.json
