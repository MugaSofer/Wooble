{
  "name": "wooble",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny search engine for Wildbow's web serials — a fan-made finder that links out to the original chapters.",
  "type": "module",
  "scripts": {
    "ingest": "node pipeline/ingest-wordpress.js",
    "build:pages": "node pipeline/build-pages.js",
    "index": "pagefind --site build --output-path site/pagefind",
    "build": "node pipeline/ingest-wordpress.js && node pipeline/build-pages.js && pagefind --site build --output-path site/pagefind",
    "serve": "node pipeline/serve.js",
    "deploy": "node pipeline/deploy.js",
    "deploy:cf": "wrangler pages deploy site --project-name wooble"
  },
  "license": "MIT",
  "devDependencies": {
    "node-html-parser": "^8.0.3",
    "pagefind": "^1.5.2",
    "wrangler": "^4.105.0"
  }
}
